export default function NavBar() {
    return (
        <nav className="nav-main">
           <ul>
               <li> Categoría A</li>
               <li> Categoría B</li>
            </ul>
        </nav>
    );
}

